import SwiftUI

// MARK: - مَجْلِس أثر
//
// شاشةٌ تُسمِع القرآن ساعاتٍ متّصلة، وتحته وقتُ الصلاة القادمة في مدينةٍ تُختار
// مرّة. التلفازُ ليس مكانًا يُقرأ فيه، بل مكانٌ يُترك فيه شيءٌ يعمل — فالتطبيق
// مشغّلٌ لا مكتبة: مسرحٌ واحد ومصدران، ولا شيء يُتصفَّح بعد أن يبدأ الصوت.
//
// وليس هذا نقلًا للتطبيق: لا مصحفَ يُقرأ ولا بحثَ ولا أذكارَ ولا مسبحةَ ولا
// قبلة. الشاشةُ المشتركة لا صاحبَ لعدّادٍ فيها، ولا بوصلةَ في تلفاز، ولا لوحةَ
// مفاتيحَ يُكتب بها عربيٌّ بمِرقاب.
@main
struct AtharTVApp: App {
    var body: some Scene {
        WindowGroup {
            TVRootView()
                .environment(\.layoutDirection, .rightToLeft)
                .preferredColorScheme(.dark)
        }
    }
}
